# GIST 604B - Python Pandas Assignment
# Minimal pytest configuration (main config in pyproject.toml)

[tool:pytest]
# This file maintained for legacy compatibility only
# Main pytest configuration is in pyproject.toml [tool.pytest.ini_options]

# Assignment-specific test collection
testpaths = tests

# Basic output settings
addopts = -v --tb=short

# Note: Full configuration including coverage, markers, and advanced options
# is specified in pyproject.toml for modern Python project standards
#
# Students can run tests with:
#   uv run pytest tests/
#   uv run pytest tests/ -v
#   uv run pytest tests/test_pandas_basics.py::test_specific_function -v
